{% block translatable_text_widget %}
    {% if form|length > 1 %}
        {% for field in form %}
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text">
                        {{ field.vars.label|upper }}
                        {#                        <img alt="{{ field.vars.label|upper }}" title="{{ field.vars.label|upper }}" src="https://unpkg.com/language-icons/icons/{{ field.vars.label|lower }}.svg" height="15" class="border"/>&nbsp;#}
                    </span>
                </div>
                {{ form_widget(field) }}
            </div>
        {% endfor %}
    {% else %}
        {{ form_widget(form|first) }}
    {% endif %}
{% endblock translatable_text_widget %}

{% block translatable_text_element_widget %}
    {% if attr['data-edit-content-input']|default(false) %}
        {% set attr = attr|merge({'data-edit-content-input': attr['data-edit-content-input']~'.'~form.vars.label|lower}) -%}
    {% endif %}
    {{ form_widget(form, {'attr':attr}) }}
{% endblock %}

{% block translatable_image_widget %}
    {% if form|length > 1 %}
        {% for field in form %}
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text">
                        {{ field.vars.label|upper }}
                        {#                        <img alt="{{ field.vars.label|upper }}" title="{{ field.vars.label|upper }}" src="https://unpkg.com/language-icons/icons/{{ field.vars.label|lower }}.svg" height="15" class="border"/>&nbsp;#}
                    </span>
                </div>
                {{ form_widget(field) }}
            </div>
        {% endfor %}
    {% else %}
        {{ form_widget(form|first) }}
    {% endif %}
{% endblock translatable_image_widget %}

{% block translatable_image_element_widget %}
    {% if attr['data-image-preview-input']|default(false) %}
        {% set attr = attr|merge({'data-image-preview-input': attr['data-image-preview-input']~'.'~form.vars.label|lower}) -%}
    {% endif %}
    {{ form_widget(form, {'attr':attr}) }}
{% endblock %}

{% block translatable_widget %}
    {% if form|length > 1 %}
        {% for field in form %}
            <div class="input-group mb-3">
                <div class="input-group-prepend">
                    <span class="input-group-text">
                        {{ field.vars.label|upper }}
                        {#                        <img alt="{{ field.vars.label|upper }}" title="{{ field.vars.label|upper }}" src="https://unpkg.com/language-icons/icons/{{ field.vars.label|lower }}.svg" height="15" class="border"/>&nbsp;#}
                    </span>
                </div>
                {{ form_widget(field,{'attr':{'class':'textarea-lg'}}) }}
            </div>
        {% endfor %}
    {% else %}
        {{ form_widget(form|first) }}
    {% endif %}
{% endblock translatable_widget %}

{% block translatable_element_widget %}
    {% if attr['data-edit-content-input']|default(false) %}
        {% set attr = attr|merge({'data-edit-content-input': attr['data-edit-content-input']~'.'~form.vars.label|lower}) -%}
    {% endif %}
    {{ form_widget(form, {'attr':attr}) }}
{% endblock %}

{% block html_class_widget %}
    <div class="input-group mb-3">
        <div class="input-group-prepend">
            <span class="input-group-text">.class</span>
        </div>
        {{ form_widget(form) }}
    </div>
{% endblock html_class_widget %}

{% block cms_color_widget %}
    <div class="input-group mb-3">
        <div class="input-group-prepend">
            <div class="input-group-text">
                <input type="checkbox" data-color-type="toggler" {{ form.vars.data ? 'checked="checked"' : '' }} />
            </div>
        </div>
        {{ form_widget(form, {'attr': {'data-color-type': 'widget'}, 'disabled': not not form.vars.data}) }}
    </div>
{% endblock cms_color_widget %}

{% block html_id_widget %}
    <div class="input-group mb-3">
        <div class="input-group-prepend">
            <span class="input-group-text">#</span>
        </div>
        {{ form_widget(form) }}
    </div>
{% endblock html_id_widget %}

{%- block polymorphic_prototype_buttons -%}
    <!-- Buttons -->
    <div id="polymorphic_prototypes" class="dropdown polymorphic-collection-prototypes">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            {#            {{ 'modules.collection.form.add.button'|trans }} {{ name }}#}
            {{ 'content.add_module.button'|trans({}, 'sfs_cms_contents') }}
        </button>
        <div class="dropdown-menu dropdown-menu-end dropdown-menu--max-height" aria-labelledby="dropdownMenuButton">
            {% for key, prototype in form.vars.prototypes %}
                <button type="button" class="polymorphic-add-button btn btn-link d-block px-2 py-1 text-start" {{ block('polymorphic_prototype_button_attributes') }}>
                    {% set trans_domain = prototype.vars.translation_domain | default(translation_domain) | default(false) %}
                    {{ not trans_domain ? prototype.vars.prototype_button_label : prototype.vars.prototype_button_label|trans({}, trans_domain) }}
                </button>
            {% endfor %}
        </div>
    </div>
{%- endblock polymorphic_prototype_buttons -%}

{% block module_collection_row %}
    <div class="module-polymorphic-collection">
        {{ form_label(form) }} {# uses: module_collection_label #}
        {{ form_errors(form) }}
        {{ form_widget(form) }} {# uses: module_collection_widget #}
    </div>
{% endblock module_collection_row %}

{% block module_collection_label %}
    <div class="row mt-3 mb-3">
        <div class="col-sm-6">
            {% set element = 'p' %}
            {% set label_attr = label_attr | merge({'class':'h4'}) %}

            {% if label is not same as(false) -%}
            {% if label is empty -%}
                {%- if label_format is not empty -%}
                    {% set label = label_format|replace({
                        '%name%': name,
                        '%id%': id,
                    }) %}
                {%- else -%}
                    {% set label = name|humanize %}
                {%- endif -%}
            {%- endif -%}
            <{{ element|default('label') }}{% if label_attr %}{% with { attr: label_attr } %}{{ block('attributes') }}{% endwith %}{% endif %}>{{ translation_domain is same as(false) ? label : label|trans({}, translation_domain) }}</{{ element|default('label') }}>
        {%- endif -%}
    </div>
    <div class="col-sm-6 text-end">
        {{ block('polymorphic_prototype_buttons') }}
    </div>
    </div>
{% endblock module_collection_label %}

{% block module_collection_widget %}
    {% set attr = attr | merge({'class':'polymorphic-collection-widget '~module_collection_class}) %}
    <!-- Nodes -->
    <div {{ block('widget_container_attributes') }}>
        {%- if form.parent is empty -%}
            {{ form_errors(form) }}
        {%- endif -%}
        {{- block('form_rows') -}}
        {{- form_rest(form) -}}
    </div>
{% endblock module_collection_widget %}

{#{% block module_collection_entry_row %}#}
{#{% endblock module_collection_entry_row %}#}

{#{% block module_collection_entry_widget %}#}
{#{% endblock module_collection_entry_widget %}#}

{#{% block module_collection_entry %}#}
{#{% endblock module_collection_entry %}#}

{% block dynamic_form_module_row %}
    <div class="polymorphic-node-row {{ row_class|default('') }}" {{ block('widget_container_attributes') }}>
        <div class="cms-module">
            <div class="cms-module-header">
                <div class="cms-module-title">
                    {#                    <span class="order-polymorphic-node" id="{{ id }}_order_index">{{ form.vars.name }}</span>##}
                    {#                    {{ form.vars.label|trans(form.vars.label_translation_parameters, form.vars.translation_domain) }}#}
                    {# form_name #}
                </div>

                <div class="cms-module-buttons">
                    <button type="button" id="{{ id }}_down_node_button" class="polymorphic-down-node-button"><span class="bi bi-chevron-down h2"></span><span class="visually-hidden">v</span></button>
                    <button type="button" id="{{ id }}_up_node_button" class="polymorphic-up-node-button"><span class="bi bi-chevron-up h2"></span><span class="visually-hidden">^</span></button>
                    <button type="button" id="{{ id }}_remove_node_button" class="btn-close polymorphic-remove-node-button ms-3"><span class="visually-hidden">x</span></button>
                </div>
            </div>
            <div class="cms-module-body">
                {{ form_widget(form) }}{{ form_errors(form) }}
            </div>
        </div>
    </div>
{% endblock dynamic_form_module_row %}

{% block dynamic_form_module_widget %}
    <div class="cms-module-edit">
        {% if edit_template|default(false) %}
            <div class="module-preview">
                {% include edit_template %}
            </div>
        {% endif %}

        <div class="cms-module-form">
            <div class="d-flex justify-content-between border-bottom bg-gray-200 p-3">
                <div>
                    <span class="order-polymorphic-node pe-3" id="{{ id }}_order_index">{{ form.vars.name }}</span>
                    <strong># {{ form.vars.label|trans(form.vars.label_translation_parameters, form.vars.translation_domain) }}</strong>
                </div>
                {# form_name #}
                <button type="button" class="btn-close" data-cms-module-form-close="" aria-label="Close"></button>
            </div>
            <div class="container">
                {% if form_template|default(false) %}
                    {% include form_template %}
                {% else %}
                    {{ form_widget(form) }}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock dynamic_form_module_widget %}

{% block container_module_row %}
    <div class="polymorphic-node-row {{ row_class|default('') }}" {{ block('widget_container_attributes') }}>
        <div class="cms-module">
            <div class="cms-module-header">
                <div class="cms-module-title">
                    <span class="order-polymorphic-node" id="{{ id }}_order_index">{{ form.vars.name }}</span>#
                    {{ form.vars.label|trans(form.vars.label_translation_parameters, form.vars.translation_domain) }}
                    {# form_name #}
                </div>

                <div class="cms-module-buttons">
                    <button type="button" id="{{ id }}_down_node_button" class="polymorphic-down-node-button"><span class="bi bi-chevron-down h2"></span><span class="visually-hidden">v</span></button>
                    <button type="button" id="{{ id }}_up_node_button" class="polymorphic-up-node-button"><span class="bi bi-chevron-up h2"></span><span class="visually-hidden">^</span></button>
                    <button type="button" id="{{ id }}_remove_node_button" class="btn-close polymorphic-remove-node-button ms-3"><span class="visually-hidden">x</span></button>
                </div>
            </div>
            <div class="cms-module-body">
                <div class="row">
                    <div class="col">{{ form_widget(form) }}{{ form_errors(form) }}</div>
                </div>
            </div>
        </div>
    </div>
{% endblock container_module_row %}

{% block container_module_widget %}
    <div class="cms-module-edit">
        {% if edit_template|default(false) %}
            <div class="module-preview">
                {% include edit_template %}
            </div>
        {% endif %}

        {% if form_template|default(false) %}
            {% include form_template %}
        {% else %}
            {{ form_widget(form) }}
        {% endif %}
    </div>
{% endblock container_module_widget %}

{% block empty_module_row %}
    <div class="polymorphic-node-row {{ row_class|default('') }}" {{ block('widget_container_attributes') }}>
        <div class="cms-modulecms-module">
            <div class="cms-module-header">
                <div class="cms-module-title">
                    <span class="order-polymorphic-node" id="{{ id }}_order_index">{{ form.vars.name }}</span>#
                    {{ form.vars.label|trans(form.vars.label_translation_parameters, form.vars.translation_domain) }}
                    {# form_name #}
                </div>

                <div class="cms-module-buttons">
                    <button type="button" id="{{ id }}_down_node_button" class="polymorphic-down-node-button"><span class="bi bi-chevron-down"></span><span class="visually-hidden">v</span></button>
                    <button type="button" id="{{ id }}_up_node_button" class="polymorphic-up-node-button"><span class="bi bi-chevron-up"></span><span class="visually-hidden">^</span></button>
                    <button type="button" id="{{ id }}_remove_node_button" class="btn-close polymorphic-remove-node-button ms-3"><span class="visually-hidden">x</span></button>
                </div>
            </div>
            <div class="cms-module-body">
                <div class="row">
                    <div class="col">{{ form_widget(form) }}{{ form_errors(form) }}</div>
                </div>
            </div>
        </div>
    </div>
{% endblock empty_module_row %}

{% block empty_module_widget %}
    <div class="cms-module-edit">
        {% if edit_template|default(false) %}
            <div class="module-preview">
                {% include edit_template %}
            </div>
        {% endif %}

        {% if form_template|default(false) %}
            {% include form_template %}
        {% else %}
            {{ form_widget(form) }}
        {% endif %}
    </div>
{% endblock empty_module_widget %}

{% block menu_item_collection_widget -%}
    {% from '@SyliusResource/Macros/notification.html.twig' import error %}
    {% set attr = attr|merge({'class': attr.class|default ~ ' controls collection-widget'}) %}

    {% spaceless %}
        <div data-form-type="collection"
            {{ block('widget_container_attributes') }}
            {% if prototype is defined and allow_add %}
            data-prototype='{{ _self.collectionItem(prototype, allow_delete, button_delete_label, '__name__')|e }}'
            {%- endif -%}>

            {{ error(form.vars.errors) }}

            {% if prototypes|default is iterable %}
                {% for key, subPrototype in prototypes %}
                    <input type="hidden"
                           data-form-prototype="{{ key }}"
                           value="{{ _self.collectionItem(subPrototype, allow_delete, button_delete_label, '__name__')|e }}" />
                {% endfor %}
            {% endif %}

            <div data-form-collection="list"
                 class="row collection-list">
                {% for child in form %}
                    {{ _self.collectionItem(child, allow_delete, button_delete_label, loop.index0) }}
                {% endfor %}
            </div>
            {% if prototype is defined and allow_add %}
                <div class="ui hidden divider"></div>
                <a href="#" class="ui labeled icon button primary add_route_path"
                   data-form-collection="add">
                    <i class="icon plus"></i>
                    {{ button_add_label|trans }}
                </a>
            {% endif %}
        </div>
    {% endspaceless %}
{%- endblock menu_item_collection_widget %}

{% block route_path_collection_widget -%}
    {% from '@SyliusResource/Macros/notification.html.twig' import error %}
    {% set attr = attr|merge({'class': attr.class|default ~ ' controls collection-widget'}) %}

    {% spaceless %}
        <div data-form-type="collection"
            {{ block('widget_container_attributes') }}
            {% if prototype is defined and allow_add %}
            data-prototype='{{ _self.collectionItem(prototype, allow_delete, button_delete_label, '__name__')|e }}'
            {%- endif -%}>

            {{ error(form.vars.errors) }}

            {% if prototypes|default is iterable %}
                {% for key, subPrototype in prototypes %}
                    <input type="hidden"
                           data-form-prototype="{{ key }}"
                           value="{{ _self.collectionItem(subPrototype, allow_delete, button_delete_label, '__name__')|e }}" />
                {% endfor %}
            {% endif %}

            <div data-form-collection="list"
                 class="row collection-list">
                {% for child in form %}
                    {{ _self.collectionItem(child, allow_delete, button_delete_label, loop.index0) }}
                {% endfor %}
            </div>
            {% if prototype is defined and allow_add %}
                <div class="ui hidden divider"></div>
                <a href="#" class="ui labeled icon button primary add_route_path"
                   data-form-collection="add">
                    <i class="icon plus"></i>
                    {{ button_add_label|trans }}
                </a>
            {% endif %}
        </div>
    {% endspaceless %}
{%- endblock route_path_collection_widget %}

{% macro collectionItem(form, allow_delete, button_delete_label, index) %}
    {% spaceless %}
        <div data-form-collection="item"
             data-form-collection-index="{{ index }}"
             class="collection-item">
            <div class="collection-box{% if form|length == 1 %} unique-field{% endif %}">
                <div class="ui grid">
                    <div class="fourteen wide column">
                        <div style="display: inline-flex; gap: 12px;">
                            {% if not form.children|length %}
                                {{ form_widget(form) }}
                            {% else %}
                                {{ form_rest(form) }}
                            {% endif %}
                        </div>
                    </div>
                    <div class="one wide column">
                    {% if allow_delete %}
                        <div class="text-right">
                            <a href="#" data-form-collection="delete" class="negative ui circular icon button remove_route_path guideLinr-element-highlight">
                                <i class="icon times"></i>
{#                                {{ button_delete_label|trans }}#}
                            </a>
                        </div>
                    {% endif %}
                    </div>
                </div>
            </div>
        </div>
    {% endspaceless %}
{% endmacro %}
